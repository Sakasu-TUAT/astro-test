<!-- <script src="https://unpkg.com/scrollreveal"></script>

<script>
    function customScrollReveal(selector: string, options: ScrollRevealOptions) {
        const defaultOptions: ScrollRevealOptions = {
            reset: true,
            distance: "60px",
            duration: 2000,
            delay: 200,
            origin: "bottom"
        };
        const mergedOptions = { ...defaultOptions, ...options };
        ScrollReveal(mergedOptions).reveal(selector);
    }
</script> -->
